notifications:
  email: false

language: ruby
cache: bundler

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.0

before_install:
  - mkdir config/environments
  - cp config/example_environment.rb config/environments/development.rb
